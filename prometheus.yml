global:
  scrape_interval: 5s # Cada 5 segundos recolectará métricas

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']